name: Test Minimal Reusable Call
on:
  workflow_dispatch: {}
jobs:
  build-match:
    uses: ./.github/workflows/debug-run.yml
    with:
      match: ""
      depth: "standard"
      model: ""
    secrets: inherit
